# Expo HAS CHANGED

Read the exact versioned docs at https://docs.expo.dev/versions/v54.0.0/ before writing any code.

Pinned to SDK 54 deliberately (not the current default template SDK) to match sibling projects HDYD/C4ME and their proven-working MediaPipe/vision-camera native modules — do not bump without re-verifying those packages first.
